FROM directus/directus:latest

# Add any custom extensions or configurations here
# For now, we'll use the base Directus image

WORKDIR /directus

# Expose Directus port
EXPOSE 8055

# Directus will start automatically via the base image's entrypoint
